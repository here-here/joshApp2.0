enum AlertAction {
  cancel,
  discard,
  disagree,
  agree,
}

const String apiURL = "https://qvrptaji88.execute-api.us-east-2.amazonaws.com/apple";
const bool devMode = false;
const double textScaleFactor = 1.0;
